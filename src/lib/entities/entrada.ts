export default class Entrada {
  identrada: number;
  valor: number;
  contribuinte: number;

    constructor (identrada: number, valor: number, contribuinte: number) {
        this.identrada = identrada;
        this.valor = valor;
        this.contribuinte = contribuinte;
    }
}